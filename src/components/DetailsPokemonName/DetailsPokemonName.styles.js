import styled from "styled-components";

export const Container = styled.div`
  display: flex;
  align-items: center;

  button {
    font-size: 1.25rem;
    background-color: transparent;
  }

  h1 {
    font-style: normal;
    font-weight: bold;
    font-size: 24px;
    line-height: 32px;
    color: var(--grass);
    margin-right: 1rem;
  }

  span {
    font-style: normal;
    font-weight: bold;
    font-size: 12px;
    line-height: 32px;
    color: var(--grass);
  }
`;
